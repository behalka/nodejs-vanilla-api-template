// eslint-disable-next-line import/no-unused-modules
export enum ENVIRONMENT {
  LOCAL = 'local',
  TEST = 'test',
  PROD = 'production',
}

export enum ROLE {
  ADMIN = 'admin',
  USER = 'user',
  ANONYMOUS = 'anonymous',
}
